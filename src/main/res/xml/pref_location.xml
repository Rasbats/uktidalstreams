<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

    <!--<SwitchPreference
        android:defaultValue="false"
        android:key="UseGPS"
        android:title="Use GPS" />-->
    <SwitchPreference
        android:defaultValue="false"
        android:key="ShowMyLocation"
        android:title="Show my location" />
    <EditTextPreference
        android:defaultValue="@string/pref_lat"
        android:key="MyLat"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="My Latitude" />
    <EditTextPreference
        android:defaultValue="@string/pref_lon"
        android:key="MyLon"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="My Longitude" />
</PreferenceScreen>
